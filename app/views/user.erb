<h1>
  <i class="fa fa-fw fa-dashboard"></i>
  <%= @results_user.name %>
</h1>
<p class="lead">
  User dashboard
</p>

<h3>Formative assessments results</h3>
<table class="table table-striped table-bordered">
  <thead>
    <tr>
      <th>Quiz</th>
      <th>Score</th>
      <th>Correct answers</th>
      <th>Date</th>
    </tr>
  </thead>
  <tbody>
    <% @best_results.each do |result| %>

      <% quiz = Quiz.get(result.quiz_id) %>
      <% result = QuizResult.get(result.id) %>
      <tr>
        <td>
          <a href="/quiz/<%= quiz.id %>">
            <%= quiz.name %>
          </a>
        </td>
        <td>
          <%= result.percentage %> %
        </td>
        <td>
          <%= result.score %> / <%= result.quiz.number_of_correct_answers %>
        </td>
        <td>
          <%= result.timestamp.strftime("%d/%m/%Y %H:%M") %>
        </td>
      </tr>

    <% end %>
  </tbody>

</table>

<p>&nbsp;</p>

<div id="user_chart"></div>

<p>&nbsp;</p>

<h3>Video Views: <b id="page_views"><%=@page_views.size%></b></h3>
<table class="table table-striped table-bordered">
  <tbody>
  <%@phases.each do |phase|
      phase['topics'].each do |topic|
        topic['videos'].each do |video| %>
          <tr>
            <td><a href="/videos/<%=video["id"]%>"><%=video["title"]%></a></td>
            <td><%if (@page_views.select { |page| page.route.to_s == "/videos/#{video["id"]}" }.size > 0) then %>
                <i class="fa fa-check"></i>
                <div class="sr-only">true</div>
              <%else%>
                <i class="fa fa-times"></i>
                <div class="sr-only">false</div>
              <%end%>
            </td>
          </tr>
        <%end
        end
      end%>
</table>


<!--Load the AJAX API-->
<script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
<script type="text/javascript">
  google.charts.load('current', {'packages':['bar']});
  google.charts.setOnLoadCallback(drawChart);

  // Callback that creates and populates a data table,
  // instantiates the pie chart, passes in the data and
  // draws it.
  function drawChart() {

    var data = google.visualization.arrayToDataTable([
      ['Quiz', 'Score'],
      <% @results.each do |result| %>
        ['<%= result.quiz.name %>', <%= result.percentage %>],
      <% end %>
    ]);

    var options = {
      chart: {
        title: 'Formative assessments results',
        subtitle: 'Latest and best results for each quiz taken',
      },
      bars: 'horizontal' // Required for Material Bar Charts.
    };

    // Instantiate and draw our chart, passing in some options.
    var chart = new google.charts.Bar(document.getElementById('user_chart'));
    chart.draw(data, options);
  }
</script>
